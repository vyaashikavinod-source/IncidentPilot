# Private ground-truth boundary

Canonical scenario ground truth is stored only in `chaos/ground_truth`. This
directory is reserved for future evaluation-only derived truth and is excluded
from application container build contexts and evidence snapshots.
